name: mimalloc_ffi
description: Dart ported well-performed and compact general memory allocator with replication of ffi package
version: 1.0.0
environment: 
  sdk: ^3.10.0
dependencies:
  code_assets: ^1.0.0
  hooks: ^1.0.0
  native_toolchain_c: ">=0.17.0 <1.0.0"
  meta: ^1.17.0
dev_dependencies:
  lints: ^6.1.0
  test: ^1.30.0
  ffigen: ^20.1.1
  ffi: ^2.2.0
